# BrainOn Frontend

의료진용 React·TypeScript 웹입니다. Node.js 24와 잠금 파일 기반 설치를 사용합니다.

## 실행

```powershell
Set-Location frontend
npm.cmd ci
npm.cmd run dev
```

기본 주소:

```text
http://localhost:5173
```

프로덕션 빌드:

```powershell
npm.cmd run build
```
